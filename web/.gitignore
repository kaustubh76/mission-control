node_modules
dist
*.local
.DS_Store
# NOTE: public/snapshot.json is generated by scripts/export_snapshot.py but is
# intentionally committed — it's the SPA's offline data fallback that Vercel's
# build bundles into dist (Vercel can't run the Python exporter).
.vercel
.env*.local
