# Crypto Trading Strategy Playbook — BNB Hack: AI Trading Agent Edition

> **Evidence-ranked strategies that actually make money in crypto, mapped onto the `ictbot` codebase, tuned for a 7-day long-only-spot DD-capped contest.**
>
> Companion to [bnb_strategy_decision.md](bnb_strategy_decision.md). Built from a 14-family research sweep (29 agents): every family got a quant-research pass with **real** academic/practitioner evidence *and* an **adversarial verifier** that reality-checked each return claim. Repo-internal grounding: [findings.md](findings.md).
>
> **📍 STATUS — SUPERSEDED by organizer ruling (BNB Chain, Gwen, 2026-06):** the contest
> **only counts trades made through the TWAK swap interface** toward P&L (it's how scoring
> tells a trade from a deposit deterministically on-chain). **Perps therefore do NOT count** —
> they can't be tracked that way, and are excluded to keep scoring fair. The earlier "perps
> permitted in-venue → run B4 at 2× on a perp DEX" recommendation in this banner is **dead.**
> What ships is the **long-only spot momentum allocator, signed end-to-end by TWAK** (the only
> thing scored) — see the LOCKED block below and [bnb_strategy_decision.md](bnb_strategy_decision.md).
>
> **🔁 DECISION UPDATE (strategy re-think):** the earlier *trend-pullback-on-ICT* pick is **dropped.** An adversarial decision panel + the repo's own [findings.md](findings.md) §13/§15 (ICT TRAIN expectancy is **negative** — BTC −0.22→−0.43R, ETH −0.29R, SOL/XRP −0.34R) show the **POI/MSS/FVG entry fails its own Gate A** — so levering it is levering a proven-zero edge, and the "80% reuse / 184 tests" comfort is false (the tests only prove the no-edge machinery *runs*). We keep only the *separable, pandas-pure* trend signal (`bias_sma` + `bias_slope`, zero POI coupling) and **delete the ICT entry machinery from the live path**, adding a simple **pullback-to-MA** entry for better fills. **Conditional:** ship only if the trend signal clears Gate A (TRAIN>0 ∧ TEST>0 ∧ ≥10–15 TEST closures on ≥2 assets ∧ worst rolling-7d DD<15%) on real **4h** basket data — else fall back to **spot long-only 1× vol-targeted trend**, never the leveraged variant unproven.
>
> **🔒 FINAL DECISION — LOCKED (supersedes everything above).** Two ground-truths closed the question:
> **(1) TWAK can't do perps for agents** (it signs spot swaps / transfers / DCA / limit-orders only) — so the B4 leveraged-perp pick is **not buildable on the official stack** and is demoted to *future work*; and **(2) the trend signal *also* failed Gate A on the actual 8-token contest universe** (`make validate_trend`) — net-negative basket expectancy **even at 0.10% friction**, and a portfolio search over **2,338 rolling 7-day windows** confirmed **no long-only strategy has a positive median weekly return** here. So the contest is variance around ~breakeven under a hard 30%-DD disqualifier. **What ships:** a **long-only spot momentum allocator** (hold top-2 of the 8 by 120-bar momentum, inverse-vol, cash-filtered) with **REGIME-ADAPTIVE deployment** — the cap is *not* frozen; it scales with a live risk-on score (breadth + trend + vol + Fear&Greed) into the participatory band **[0.40, 0.85]**, deploying in a risk-on week and pulling to cash in a risk-off one. Executed via **TWAK as sole signer** — **DQ-proof** (worst-week DD ~17.6% « 30%), **active** (~11.5 trades/wk » 7), keeps most of a static-0.85's upside at lower DD. Because a backtest can't validate a *forward* week, it's **validated forward in paper** (`make forward_report`, daily now → contest) — the backtest is only a cross-regime sanity check. Full record: **[bnb_strategy_decision.md](bnb_strategy_decision.md)**. Validate: `make validate_allocator`. Everything in §3–§7 and Part B below is retained as the **research audit trail**, not the deploy target.
>
> **📊 VALIDATION UPDATE — gate + scoreboard (2026-06-14).** PnL + win-rate are now first-class
> validation outputs, framed honestly: risk-first **survival stays the hard pass/fail GATE**
> (worst-week DD < 25% AND ≥ 7 trades/wk — an arm must clear it to be ranked at all), and PnL +
> win-rate is a **labelled SCOREBOARD over the survivors**, never an edge claim. A backtest
> total-return ranking over the long trending sample is dominated by **regime luck, not a repeatable
> edge** ([bnb_strategy_decision.md](bnb_strategy_decision.md) §1); the contest-length **window**
> win-rate (share of rolling 7-day windows positive) and the live **day** win-rate (up-days /
> decided-days, matching the dashboard `pnl.ts`) are the decision-relevant numbers. Every Top-10
> family is now wired to its implemented + validated arm in **§11** (auto-generated by `make
> playbook`, parity-enforced by `tests/test_playbook_parity.py`); the per-arm PnL/win-rate
> scoreboard lives in `make campaign`. **No new strategies** were added — this is audit + wiring +
> measurement.

---

## 1. Executive summary — read this even if you read nothing else

You asked for strategies that "make 30–40% return." Here is the uncomfortable, load-bearing finding:

> **Every one of the 14 verified strategy families came back `plausible_30_40pct_in_7day = FALSE`.**

That is not pessimism — it is the arithmetic of a **7-day, long-only, spot, drawdown-capped** window. A +35% week on long-only spot is only reachable through:
1. **Leverage** — *unavailable on spot*;
2. **All-in concentration** on one token catching a parabola — a coin-flip on regime luck, not edge. The same concentration that prints +35% in a good week prints a **>30% disqualifying drawdown** in a reversal week; or
3. **Survivorship / zero-cost backtest fantasy** — the famous "180% APR grid bot" ran 10× leverage + zero fees; the "400% opening-range-breakout" and "228% Keltner" numbers exclude costs entirely; any "30–40%/month rotation" claim is survivorship-inflated (dead coins silently dropped, +50–200% inflation).

**The binding constraint in this contest is the drawdown gate, not the return target.** In a field where most entrants blow the 30% gate chasing a hero number, a **flat-to-+10% week with DD <15% places well**. So the winning objective is the *opposite* of swinging for 30–40%:

- **(a)** clear the **≥7-trade minimum**,
- **(b)** keep **max DD ≤15%** so you survive to be ranked at all,
- **(c)** post a **modest positive number**.

This is also precisely why the recommendation does **not** drag you back to the no-edge scalp. The deployable pick (§6) is a fundamentally different animal — **1h/4h trend + pullback, long-only spot, RR≥2, vol-targeted** — versus the **1m fade/follow ICT scalp** that [findings.md §15](findings.md) proved has negative out-of-sample expectancy after friction. Same *primitives*, completely different *setup*.

**Honest expected outcome for the recommended strategy: a 7-day distribution of roughly −10% to +15%, centered near zero, dominated by whether the contest week trends up.** A clean risk-on week could plausibly print +10–15%. Plan for that, not for a moonshot.

---

## 2. Methodology & ranking criteria

Each family was scored for **this format's** binding constraints, in priority order:

1. **Long-only-spot executable** — no shorting / perps / funding leg (the BSC venue cannot short).
2. **Friction-robust** — survives a **~0.5–4% PancakeSwap round-trip** with **RR ≥ 2**. (Per [findings.md](findings.md), only RR≥2 configs survived even *perp* friction of ~0.14% round-trip; AMM friction is **3–7× worse**.)
3. **Stays inside the ≤15% / 30% DD gate.**
4. **Fits 1h/4h bars** and can produce **≥7 trades** in 7 days.
5. **Maximal reuse** of existing, tested `ictbot` primitives.

Evidence was credibility-graded: **academic > reputable practitioner > exchange/marketing**. Hype was actively distrusted — survivorship bias inflates crypto backtests **50–200%** (>58% of all listed tokens are "dead"; only 9 of the Dec-2016 top-100 remain in the top-100 in 2024), and nearly every "X%/month bot" claim assumes zero cost and/or leverage.

**The `ictbot` primitives referenced throughout** (all real, tested):
- **Indicators** [`src/ictbot/indicators/`](../src/ictbot/indicators/): `atr`, `delta`/CVD, `fvg`, `liquidity` (swing highs/lows + `get_next_liquidity_level`), `mss` (market-structure shift), `poi_order_block` / `poi_min_max`, `structure` (`find_swings`, `get_swing_bias`), `bias_sma` / `bias_slope`, `regime` (`atr_percentile_regime` → HIGH/LOW/NORMAL vol), `mitigation`, `risk` (`calculate_rr`), `tick`.
- **Strategy** [`strategy/ict_pro_max.py`](../src/ictbot/strategy/ict_pro_max.py): `ICTProMaxStrategy(bias_engine, poi_engine, strategy_mode, sl_anchor, structural_tp1_rr, sl_atr_mult, tp_atr_mult, skip_in_low_vol, fib_filter, …)`; `evaluate()` → 25-key dict with `entry`/`sl`/`tp`/`tp2`/`rr`/`confidence`.
- **Backtester** [`engine/backtest.py`](../src/ictbot/engine/backtest.py): `run_backtest(pair, bars, …, fee_per_side, slippage_per_side, start_idx, end_idx)` — walk-forward capable, with a friction model.
- **Risk caps** [`portfolio/caps.py`](../src/ictbot/portfolio/caps.py): `MaxOpenPositions`, `MaxConcurrentSameDirection`, `DailyLossLimit`, `MaxDrawdown`, `MaxLiveTradesPerDay`, `NearPriceDedup`, `NewsBlackoutCap`; `Account` tracks equity + drawdown.
- **Execution** [`exec/`](../src/ictbot/exec/): `broker.py` protocol, `Order` struct, `PaperBroker`, `binance_live`, `delta_live`. **No CMC adapter and no BSC/PancakeSwap broker exist yet** — these are the two build gaps (§8).

---

## 3. The ranked Top 10 (for THIS hackathon)

| # | Strategy | Fit /10 | Honest 7-day return\* | Long-spot? | Friction | One-line verdict |
|---|----------|:---:|----------------------|:---:|:---:|------------------|
| **1** | **Trend-Following Pullback** (buy-the-dip in a confirmed uptrend) | **7** | −10% … +15% | ✅ | med | **Deploy.** Best long-spot fit; reuses ~80% of follow-mode pipeline; the trend filter *is* the DD control. |
| **2** | **Time-Series Momentum / MA Trend** (4h, long/flat) | **6** | −10% … +12% | ✅ | med-hi | **Runner-up.** Signal already coded; strongest peer-reviewed crypto anomaly; lowest build. |
| **3** | **Dual Momentum** (absolute + relative, cash-out) | **6.5** | −10% … +12% | ✅ | low | Co-primary fallback; "rotate to stablecoin" is native long-only risk-off. |
| **4** | **Volatility Breakout** (Keltner/ATR-band, Donchian ORB) | **6** | −8% … +12% | ✅ | high | Trend-capture leg; best TA evidence (Donchian Sharpe ~1.5) but wide stops fight the trade minimum. |
| **5** | **Grid / Range Trading** | **6** | −5% … +6% | ✅ | high | Auto-hits ≥7 trades; **negative skew** — one breakout blows the gate. Ranging-week backup, hard stop mandatory. |
| **6** | **Short-Horizon Mean Reversion** (RSI-2 / Bollinger / z) | **6** | −10% … +12% | ✅ | high | Survived real fees in indie tests, but reversal *flips to momentum* on liquid majors; thin edge dies to AMM cost. Hedge only. |
| **7** | **MA Crossover** (EMA / golden-cross) | **5** | −15% … +15% | ✅ | high | Lowest build, but [findings.md §15](findings.md) killed this exact setup OOS. Use as a **trend filter**, never standalone. |
| **8** | **Cross-Sectional Momentum / Rotation** | **4** | −10% … +12% | ✅ | high | Real but the *weaker* momentum form; 7-day rule forces it off its 5–7d cadence; genuine 30%+ DD tail; most new code. |
| **9** | **Donchian / Turtle Breakout** | **4** | −8% … +12% | ✅ | high | Most academically credible — but every number is daily/multi-week; a 7-day window amputates the fat-right-tail mechanism. |
| **10** | **Volatility-Targeting overlay** | **4** | n/a (overlay) | ✅ | low | Cheap DD-gate insurance (−25–40% DD) but generates **zero entries** — wire as an overlay on #1, not standalone. |

\* After-AMM-friction range from the adversarial verifiers; centered near zero, dominated by whether the contest week trends. **No family produced a credible path to 30–40% in 7 days.**

---

## 4. Per-strategy deep dives

Each entry: **Mechanism** → **Evidence** → **Realistic performance (after costs)** → **Crypto fit** → **Codebase mapping** → **Verdict**.

---

### #1 — Trend-Following Pullback (buy-the-dip in a confirmed uptrend) · Fit 7/10 · **SUPERSEDED**

> **Superseded by the Decision Update (top of doc).** The pullback *concept* is kept, but its **ICT POI/MSS/FVG entry implementation was dropped** — that machinery has negative OOS expectancy ([findings.md](findings.md) §13/§15). The deployable pick is now the pure trend signal (`bias_sma`+`bias_slope`) with a simple pullback-to-MA entry (§6 / B4). Read the rest of this entry for the trend-following rationale, not the ICT entry mechanics.

**Mechanism.** Two-stage long-only gate on liquid majors. *Stage 1 — trend filter:* only act when the asset is in a confirmed uptrend (higher-highs/higher-lows + price above a rising slow MA) and not in dead-low volatility. *Stage 2 — pullback trigger:* wait for a retrace into a demand zone / OTE discount, then buy the bullish reclaim. Structural stop just below the zone; take-profit at RR≥2; trail to break-even after +1R. Timeframe 1h entry with 4h/1d trend context.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Liu & Tsyvinski, *Risks and Returns of Cryptocurrency*, RFS 34(6) 2021 | academic | high | Crypto momentum/trend is a robust, priced return predictor across horizons. |
| Liu, Tsyvinski & Wu, *Common Risk Factors in Cryptocurrency*, JoF 2022 | academic | high | A 3-factor (market/size/**momentum**) model prices the cross-section — momentum is a genuine factor, not noise. |
| Rohrbach, Suremann & Osterrieder, *Momentum and Trend Following for Currencies and Bitcoin*, SSRN 2949379 | academic | high | TSMOM/EMA trend systems are profitable on BTC; Sharpe ~1.2–1.7 pre-cost. |

**Realistic performance (after AMM costs).** ~**10–30%/yr in a favourable trending regime**, near-0 or negative in chop. Sharpe ~1.2–1.7 pre-cost, materially lower after. **Max DD held to ~5–15%** by the trend filter + structural stops + 0.5–1% risk/trade — which is exactly the property the contest rewards. Win-rate moderate; positive expectancy comes from RR≥2, not hit-rate.

**Crypto fit.** Best in a **broad risk-on uptrend with clean pullbacks**; the trend filter self-disables in downtrends/chop (sits in stables), which is the built-in drawdown defence.

**Codebase mapping.** *Reuse (~80%):* `bias_engine='swing'` (`structure.get_swing_bias`), `poi_engine='order_block'` (`poi_order_block.get_ob_poi`, `fib_filter=0.5`), `mss.get_ltf_mss`, `sl_anchor='structural'` + `structural_tp1_rr=2.0`, `regime.atr_percentile_regime` (`skip_in_low_vol=True`), `get_next_liquidity_level` for TP2, the full caps stack, `Account` DD tracking, the WFO backtester. *Build-new:* the CMC adapter + BSC broker (§8), a long-only switch, and a 100–200h MA confirmation.

**Verdict.** **The deployable choice.** Long-only-spot native, friction-robust at RR≥2, self-DD-controlling, and reuses the most tested code. Full spec in §6.

---

### #2 — Time-Series Momentum / MA Trend-Following (4h, long/flat) · Fit 6/10 · **RUNNER-UP**

**Mechanism.** Per coin: go long when trailing return is positive / price is above a moving average; otherwise sit flat (in stables). No counter-trend, no shorting. Diversify the long/flat signal across the whole ~8–10-coin basket — **the diversification is the edge**, not any single coin. Run on **4h** bars.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Moskowitz, Ooi & Pedersen, *Time Series Momentum*, JFE 2012 | academic | high | TSMOM is a pervasive, significant effect across asset classes; diversified Sharpe ~1.3. |
| Man AHL, *In Crypto We Trend* (2024) | practitioner | high | 50/200-day trend on crypto: **single-coin Sharpe is small; a diversified multi-coin sleeve is where the Sharpe comes from.** |
| arXiv 2009.12155 (intraday BTC trend) | academic | high | **No profitable *intraday* BTC trend after costs → run the signal on 4h/daily, not 1h.** |

**Realistic performance.** Single-name ~0–30%/yr net; a diversified ~10-coin sleeve is steadier with a better Sharpe (CTA-industry-like **0.4–1.0 net** on daily signals). **Strategy-level DDs of 20–40% are normal** for unmanaged crypto trend (it gives back open profit on reversals) — so vol-targeting + `MaxDrawdown(0.15)` are mandatory.

**Crypto fit.** Sustained directional trends; weak in choppy ranges (whipsaw). The long/flat structure maps cleanly to long-only spot + the DD cap.

**Codebase mapping.** *Reuse:* `bias_sma`/`bias_slope` (signal **already shipped**; `strategy_mode='follow'` is the in-repo default that beat fade), `regime`, `NearPriceDedup`, caps, account. *Build-new:* basket scheduler on 4h + the shared CMC adapter / BSC broker. **Near-zero net-new strategy code.**

**Verdict.** **Lowest-build, highest-evidence backbone.** Pair it with #1's pullback entry, or fall back to it wholesale. Run on **4h, not 1h.**

---

### #3 — Dual Momentum (absolute + relative, with cash-out) · Fit 6.5/10 · **CO-PRIMARY FALLBACK**

**Mechanism.** Combine *absolute* momentum (only be in-market when the asset's own trailing return is positive — else **rotate to stablecoin**) with *relative* momentum (own the strongest 1–3 names). The cash-out leg is a native long-only risk-off switch — no shorting required.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| AUT/ACFR crypto TSMOM working paper | academic | high | Crypto time-series momentum with a risk-off filter improves risk-adjusted return materially. |
| Dobrynskaya, *Cryptocurrency Momentum and Reversal* (HSE/SSRN 3913263) | academic | high | Momentum is significant short-horizon; **reverses beyond ~1 month** — keep the holding window short. |
| Antonacci, *Dual Momentum Investing* (framework) | practitioner | medium | Absolute+relative momentum historically cuts drawdown vs. relative-only. |

**Realistic performance.** ~**0–30%/yr** for a well-run absolute+relative overlay on majors after costs; Sharpe 0.5–1.5. Classic monthly dual-momentum ran ~11% DD, but **concentrated crypto single-name with no intra-period stop can blow far past that** — keep it diversified + stopped.

**Crypto fit.** Trending markets with leadership rotation; the cash-out leg shines in downturns. Compressed to a 7-day window it loses some edge (it's a daily/weekly idea) but the risk-off behaviour is still valuable.

**Codebase mapping.** *Reuse:* `regime`, `atr`, caps, account. *Build-new:* a small cross-sectional ranker (rank trailing return across the basket) + the cash-out rule. **The non-trending-week insurance policy** for the #1 pick.

**Verdict.** Arm it as the **fallback for a chop week**: when #1's trend filter sits flat, rotate to the single strongest positive-momentum name or fully to stables.

---

### #4 — Volatility Breakout (Keltner/ATR-band, Donchian opening-range) · Fit 6/10

**Mechanism.** Enter long when price expands beyond an ATR/volatility band or a prior range high (regime-shift capture). Wide ATR stops; trail with the band.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Zarattini, Pagani & Barbon, *Catching Crypto Trends*, SFI RP25-80 (2025) | academic | high | Tactical breakout/trend on BTC + alts delivers strong risk-adjusted returns; diversified Donchian Sharpe ~1.5. |
| Time-series-momentum crypto papers (ScienceDirect; AUT) | academic | high | Breakout-style momentum persists on daily crypto data. |
| PancakeSwap docs (0.25%/swap + 0.5–5% auto-slippage) + BSC gas | exchange | high | **Real round-trip cost ~1–4%** — the number that re-costs every backtest here. |

**Realistic performance.** ~**10–40%/yr** for a clean single-strategy breakout in a neutral-to-trending cycle; Sharpe ~0.7–1.5 realistic (>1.5 figures are pre-cost). Strategy-level **DD 15–30%** from strings of false breaks — must be capped with vol-target sizing + `MaxDrawdown`.

**Crypto fit.** Excellent for catching the *start* of a move; bleeds in chop via false breakouts. Wide stops + few trades **fight the ≥7-trade minimum** on a 7-day window.

**Codebase mapping.** *Reuse:* the `atr` + `rr2plus` grids, `regime` gate, liquidity levels for targets, caps. *Build-new:* a Donchian/Keltner band entry (small) + shared adapter/broker.

**Verdict.** A **trend-capture leg, not the headline** — bolt it on for breakout days, but the trade-minimum and AMM friction keep it off the top spot.

---

### #5 — Grid / Range Trading (laddered mean-reversion) · Fit 6/10

**Mechanism.** Place a ladder of buy/sell levels across a defined range; profit from oscillation as price bounces between rungs. Naturally generates many fills.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Stevens Institute FE800, *Improving Grid Trading Strategies in Bitcoin* | academic | medium | Grid market-making is profitable **in genuinely range-bound regimes**. |
| Bitsgap / Paybis backtesting writeups | practitioner | medium | Realistic-fee grid returns are modest; performance collapses out-of-range. |
| Bitsgap / Phemex on the breakout failure mode | exchange | high | **A trend breakout routinely produces 30–60%+ unrealized DD** without a hard range stop. |

**Realistic performance.** ~**8–25%/yr unleveraged in a true range**, flat-to-negative otherwise; Sharpe 0.5–1.5 in-regime. **Negative skew is the danger:** without a hard stop, one breakout blows the 30% gate. With a stop, capped ~≤15%.

**Crypto fit.** Only in a sideways week — and you cannot know in advance the contest week will range. **Gate-fragile.**

**Codebase mapping.** *Reuse:* `regime.atr_percentile_regime` as the *pause* gate (stop gridding when vol regime breaks), caps. *Build-new:* the grid engine + a **non-negotiable poll-and-exit range stop**.

**Verdict.** **Ranging-week backup only**, and only with a hard stop. Auto-satisfies the trade minimum, but the negative skew is exactly the wrong risk profile for a DD-gated contest.

---

### #6 — Short-Horizon Mean Reversion (RSI-2 / Bollinger / z-score) · Fit 6/10

**Mechanism.** Buy oversold dips on majors (RSI-2 low / lower-Bollinger / negative z-score), exit on reversion to the mean.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Fičura (2023), *Size and volume on crypto momentum and reversal*, VSE FFA WP | academic | high | Short-term reversal exists — but **inverts to momentum on large, liquid, high-volume names** (exactly this universe). |
| Zaremba, Bianchi & Mikutowski (2021), *Up or down?*, Int. Rev. Financial Analysis | academic | high | Short-term reversal and momentum effects are liquidity-dependent in crypto. |
| StratProof, *22 strategies on real fees, 10 days* | practitioner | medium | Mean-reversion was among the few that survived **real fees** in a short live paper-trade. |

**Realistic performance.** After 0.5% AMM round-trip: ~**−10% to +25%/yr**; Sharpe 0.5–1.2 on majors. **DD 15–35%** — it *buys falling assets*, so a trend day stacks losers. The per-trade edge is thin and **erased by AMM friction**.

**Crypto fit.** Ranging markets on the very assets where the literature says reversal **flips to momentum** — a structural headwind for this exact universe.

**Codebase mapping.** *Reuse:* `atr` for stop distance, `regime`, caps, `NearPriceDedup` (critical — stops re-buying the same dip on noise). *Build-new:* RSI-2/Bollinger/z-score entry.

**Verdict.** **Ranging-week hedge only.** The reversal-flips-to-momentum result on liquid majors plus AMM friction makes it unreliable as a primary.

---

### #7 — Moving-Average Crossover (EMA / golden-cross) · Fit 5/10

**Mechanism.** Fast MA crosses slow MA → long; opposite cross → flat. The textbook trend signal.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| quant-signals EMA(9/21), 6 assets / 2,716 trades | practitioner | medium | Daily BTC EMA crossover: profit factor ~1.59 — but headline Sharpe figures are sample-specific. |
| quantifiedstrategies / statsedge golden-cross | practitioner | medium | Golden/death-cross often **lags buy-and-hold** despite high win-rate. |
| **[findings.md §15](findings.md) (this repo)** | repo | high | **This exact crossover/ICT setup had NO robust OOS edge after friction.** |
| arXiv 2602.10785 (intraday EMA walk-forward) | academic | high | Intraday EMA-crossover robust-Sharpe is **highly unstable** OOS. |

**Realistic performance.** ~**−10% to +25%/yr**, typically lagging buy-and-hold in bull years; Sharpe 0.3–1.0 on 1h after costs. Its one genuine virtue is **drawdown reduction vs. hold** (15–25% DD vs. buy-and-hold's deeper).

**Crypto fit.** Persistent trends only; whipsaws in chop. **Your own repo data says it has no standalone edge.**

**Codebase mapping.** *Reuse:* `bias_sma`/`bias_slope` ship today. **Use it as the trend *filter* feeding #1's entry — never as the entry itself.**

**Verdict.** **Filter, not strategy.** Lowest build, but standalone it's the thing findings.md already disproved. Its role is the 100–200h trend confirmation in §6.

---

### #8 — Cross-Sectional Momentum / Relative-Strength Rotation · Fit 4/10

**Mechanism.** Each rebalance, rank the universe by trailing return and hold the top-K strongest, rotating periodically.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Han, Kang & Ryu (2023), *TS & CS Momentum in Crypto under Realistic Assumptions*, SSRN 4675565 | academic | high | **CS momentum is the *weak* form** under realistic costs/liquidation; many significant-looking CS portfolios earn insignificant profit after reality. |
| Dobrynskaya (2024), *Crypto Momentum and Reversal*, SSRN 3913263 | academic | high | CS momentum works only on **2–4 week** horizons, then **reverses** and inflicts large losses. |
| Starkiller Capital, *CS Momentum in Crypto* | practitioner | medium | In-sample +69%/yr → **OOS −2.35%/yr**; DD **>75%**; 125bps round-trip makes it underperform the benchmark. |

**Realistic performance.** Liquid-majors long-only after costs: ~**0–25%/yr with wide variance**, many windows flat-to-negative. **Structurally high DD (40–75%)** without an absolute-momentum overlay. Positively skewed (a few winners carry it) — wrong profile for a 7-day total-return contest.

**Crypto fit.** Needs a broad trending market with clear leader/laggard dispersion; fails in chop and crashes on loser-rebounds.

**Codebase mapping.** *Reuse:* backtester friction model, ATR sizing, regime, caps. *Build-new:* a **cross-sectional ranker (does not exist** — current strategy is strictly single-pair) + rotation scheduler. **Most net-new code of any candidate.**

**Verdict.** Real effect, **wrong instrument for this window**: the 7-day/7-trade rule forces it off its only profitable 5–7d cadence into high-friction rotations, it carries genuine 30%+ DD gate-breach risk, and it needs the most new code.

---

### #9 — Donchian Channel / Turtle Breakout · Fit 4/10

**Mechanism.** Enter on an N-day high breakout, exit on the opposite channel. The classic Turtle system — profit comes from **holding winners across weeks** for the fat right tail.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Zarattini, Pagani & Barbon (2025), *Catching Crypto Trends*, SFI RP25-80 | academic | high | Diversified multi-lookback **daily** Donchian on crypto: ~25–35% CAGR, Sharpe ~1.3–1.6, DD ~19–25%. |
| de Iriarte (2023), *Turtle Trading in Crypto* | practitioner | medium | Turtle works on crypto **over long daily samples**. |
| QuantPedia, *Trend-following & Mean-reversion in Bitcoin* | academic | medium | N-day breakout edges on BTC are real but **daily/multi-week**. |

**Realistic performance.** Daily ensemble ~25–35% CAGR / Sharpe 1.3–1.6 / DD 19–25% — **every figure is daily/multi-week.** Over a 7-day window of intraday single-name breakout: a bad week of whipsaws can be a string of −1R losses with no time for the right tail to pay.

**Crypto fit.** Excellent on daily horizons; **structurally amputated by a 7-day window** — its profit mechanism (let winners run for weeks) cannot operate.

**Codebase mapping.** *Reuse:* `atr`/`rr2plus` grids, liquidity levels, caps. *Build-new:* Donchian channel entry.

**Verdict.** Most academically credible of the breakout family, but **horizon-mismatched** to this contest. [findings.md](findings.md) also shows thin breakout edges die to friction at 7bps — well before the 7–15× worse AMM cost.

---

### #10 — Volatility-Targeting / Risk-Parity Sizing Overlay · Fit 4/10 (overlay)

**Mechanism.** Not a signal — a **sizing overlay**. Scale position inverse to realized volatility so exposure (and drawdown) stays stable; de-lever into high-vol regimes.

**Evidence.**
| Source | Type | Cred | Claim |
|---|---|---|---|
| Harvey, Hoyle, Rattray et al. (2018), *The Impact of Volatility Targeting*, FAJ | academic | high | Vol targeting improves Sharpe and **reduces drawdown** across asset classes. |
| QuantPedia, *Introduction to Volatility Targeting* | practitioner | high | Worked 60/40 example: Sharpe 0.79 → 1.02; meaningful max-DD reduction. |
| Man Group, *Crypto: Too Hot to Handle?* | practitioner | high | Scaling BTC to a target vol adds risk-adjusted return over a long sample. |

**Realistic performance.** Adds **+0.2–0.5 Sharpe** and **25–40% relative DD reduction** to an *existing* edge. **Generates zero return on its own.**

**Crypto fit.** Universally helpful as a risk overlay; on long-only spot it can only *de-lever* (forfeiting some of its documented gain), and a 7-day window is short for its edge to show.

**Codebase mapping.** *Reuse:* `atr`, `regime`, the sizing path, caps. *Build-new:* an inverse-vol scalar clamped ≤1.0.

**Verdict.** **Wire it as an overlay on the #1 pick**, not as a strategy — it cannot meet the ≥7-trade minimum alone, but it is cheap, evidence-backed DD-gate insurance.

---

## 5. Excluded from the Top 10 — venue-incompatible (and why we say so explicitly)

These are legitimate, sometimes excellent strategies — but the **BSC spot, long-only** venue structurally cannot run them. Documented here so nobody wastes a day building one.

> **Update:** these four would only become viable with perps/shorting — see **[Part B — Perps & Futures](#part-b--perps--futures-shorting--leverage)** below. *(NON-SCORING: per the organizer ruling, only TWAK-swap-interface trades count toward P&L, so perps contribute zero — Part B is research/future-work, not a contest option.)*

| Family | Why it's out |
|--------|--------------|
| **Funding-Rate Carry / Cash-and-Carry Basis** | Needs a **short perp leg** (long spot + short perp) — impossible on spot-only. Even if it ran, carry pro-rates to only **~0.1–0.4% over 7 days** → structurally zero contest-relevant return. (BIS WP 1087; Fan et al. SSRN 4666425.) |
| **Pairs / Statistical Arbitrage (e.g. ETH/BTC)** | Requires **shorting** one leg; structural-break risk; realistic OOS Sharpe far below the curated 1.5–2.5 academic claims. |
| **Multi-Factor combo (momentum + carry + low-vol)** | Its diversifying legs (carry, low-vol) need **shorting**; long-only it collapses to market beta with 30–60% DD. (Liu/Tsyvinski/Wu JoF 2022 confirms the factors exist — but you can't harvest the short side here.) |
| **Sentiment / Flow (Fear&Greed contrarian, funding-as-signal, netflows)** | Long-only through a downtrend = **20–40% DD** (extreme fear can deepen for months — 2018, 2022). Best value is as a **regime *filter*, not a standalone strategy** — fold a Fear&Greed / funding-extreme check into #1's trend gate. |

---

## 6. THE DEPLOYABLE PICK — Trend-Following hybrid (long-only spot, 1×)

> **What to ship (spot leg):** a diversified **time-series trend-following** core across 6–8 liquid majors, long-only on PancakeSwap spot, with a **simple pullback-to-the-MA entry** for friction-reducing fills. **The ICT POI/MSS/FVG entry machinery is dropped** — [findings.md](findings.md) §13/§15 proves it has negative OOS expectancy after friction. Optimize to survive the DD gate + clear ≥7 trades, not for a hero return.

### Entry — per coin, on each closed 4h candle
- **Direction:** BULLISH only if `bias_sma.get_htf_bias` (SMA20>SMA50) **AND** `bias_slope.get_slope_bias` (EMA-linreg slope>0) agree up; skip on disagreement. Skip when `regime.atr_percentile_regime == LOW_VOL` (chop).
- **Timing (the hybrid layer):** don't chase the breakout — wait for a **pullback to the bias-MA**, enter on the first 4h bar that closes back above the rising MA. Buys the discount → cuts entry slippage (friction is the dominant P&L term in this repo).
- **No `poi_tap` / `ltf_mss` / `fvg` / `delta`** in the gate — those are deleted from the live path.
- Route through `CapGate`; the **≥7-trade minimum is met structurally** by the 6–8-name basket, not by a forced-trade hack.

### Exit — poll-and-exit (no native AMM stops)

| Trigger | Action |
|---|---|
| **SL** | ATR-anchored: entry − `sl_atr_mult`×ATR (≈1.5–2.0×, tuned in WFO). |
| **TP** | ≥2R via `Order(tp)`. |
| **Trend-flip** | 4h bias flips bearish → exit to stable (don't give a winner back). |
| **Trail** | move SL to **break-even after +1R**. |

### Sizing
- **ATR vol-targeting** via `sizing.position_size` — equal risk per leg regardless of price/vol.
- Risk **0.5–1.0% equity per leg**; **half-Kelly (`kelly_position_size`) as an upper clamp only**, never to scale up. Spot = **1×**, no leverage.

### Reuse vs. build
**Reuse (pure, no edge concern):** `bias_sma`/`bias_slope`, `structure` swing bias, `atr`, `regime`, `sizing`, the full `CapGate` (override `MaxDrawdown` 0.20→**0.15**), `Account`, `Order`/`PaperBroker`. **Build-new:** a `trend_basket.py` strategy (zero ICT imports); a **strategy-agnostic WFO replay loop** (the gating item — `wfo.py`/`run_backtest` are POI-param-bound and can only validate ICT configs today); the CMC 1h→4h adapter; the PancakeSwap spot broker via TWAK. **Delete from the live path:** the POI/MSS/FVG/delta AND-gate at `ict_pro_max.py` L409–426.

### Honest expected outcome
7-day ≈ **−4% to +12%**, centered **+1–4%** in a trending week, ~flat in chop. The edge is real (the one peer-reviewed crypto anomaly), but 7 days is trend-following's *weakest* horizon → this buys *odds tilted positive + survive variance + clear ≥7 trades*, **not** a guaranteed print. **Ship only if it clears Gate A on real 4h data; else run the same signal at conservative sizing — never deploy unproven.**

---

## 7. Runner-up + non-trending-week fallback

- **Runner-up — Time-Series Momentum / MA Trend (4h, long/flat-to-stable):** lowest build of any candidate (signal already shipped via `bias_sma`/`bias_slope`, follow mode), high-credibility evidence (MOP Sharpe 1.31; Man AHL "diversification is the edge"). **Run on 4h, not 1h** (no profitable intraday BTC trend). Vol-target + `MaxDrawdown(0.15)` + `NearPriceDedup` on. The steady, friction-robust backbone to **pair with** or **fall back to** the #1 pullback entry — same primitives, same DD machinery.
- **Non-trending-week fallback — Dual Momentum cash-out (#3):** if the trend filter sits flat all week, rotate to the single strongest positive-momentum name, or **fully to stables**. Native long-only risk-off, friction-light. Keep it armed.

---

## 8. Non-negotiable pre-deploy gates (= the verification plan)

The rankings **hinge** on these. Do not skip any.

1. **Re-cost the backtest to AMM reality.** The repo default is `fee_per_side=0.0005 + slippage_per_side=0.0002` (**0.14% round-trip — a perp assumption**). Raise to PancakeSwap reality: `fee_per_side ≥ 0.0025` plus a slippage + gas model (**~0.5–4% round-trip**). **If the edge does not survive the re-cost, do not deploy.**
2. **Set `MaxDrawdown(account, limit=0.15)` explicitly** — the in-code default is **0.20**, not your declared 0.15. Verify in [`portfolio/caps.py`](../src/ictbot/portfolio/caps.py).
3. **Build the two missing pieces** (neither exists today — `data/` has only Binance/Delta; `exec/` has only `binance_live`/`delta_live`/`paper`):
   - a **CoinMarketCap 1h OHLCV adapter** implementing the `Exchange` protocol, and
   - a **BSC / PancakeSwap broker via Trust Wallet Agent Kit** implementing the `broker.py` interface with **poll-and-exit** (an AMM can **gap between 1h polls** — untested poll-exit logic is itself a primary drawdown source on a single-shot 7-day window).
   - **Validate both on `PaperBroker` + WFO first.**
4. **Add the long-only switch** (the gate already separates bullish/bearish setups) + the 100–200h MA confirmation.
5. **Re-tune via the `rr2plus` / `atr` grids with the RR≥2 hard floor at the new AMM friction**, then walk-forward to the acceptance gate: **TRAIN expectancy > 0 AND TEST expectancy > 0 AND worst rolling-7d DD < 15% AND ≥7 trades/week.**
6. **Keep the Dual-Momentum cash-out fallback armed** for a non-trending week.

---

## 9. How this maps to the hackathon scoring

- **Track 1 (live PnL, DD gate):** the trend filter + structural stops + trail-to-BE + explicit `MaxDrawdown(0.15)` are engineered to keep you **inside the gate** so you are ranked at all. In a field that mostly blows the 30% gate chasing hero returns, **a modest positive number with DD <15% places** — that is the whole game.
- **≥7-trade minimum:** met by rotating quality signals across the 8-coin basket (not by lowering quality), with the forced-trade fallback as backstop.
- **TWAK Best-Use special:** unchanged and still your highest-EV side prize — this playbook **strengthens** the "careful autonomous loop" narrative (a self-custody agent that *sits in stables unless three conditions align*) rather than replacing it.

**Bottom line:** the contest is won by *surviving*, not by swinging. Ship #1, keep DD under 15%, clear 7 trades, and let the field's blow-ups carry you up the ranking.

---

## 10. References

All sources are real and checkable; credibility graded inline above.

**Academic (high):**
- Liu & Tsyvinski (2021), *Risks and Returns of Cryptocurrency*, Review of Financial Studies 34(6) / NBER w24877.
- Liu, Tsyvinski & Wu (2022), *Common Risk Factors in Cryptocurrency*, Journal of Finance.
- Moskowitz, Ooi & Pedersen (2012), *Time Series Momentum*, Journal of Financial Economics.
- Rohrbach, Suremann & Osterrieder, *Momentum and Trend Following for Currencies and Bitcoin*, SSRN 2949379.
- Han, Kang & Ryu (2023), *Time-Series and Cross-Sectional Momentum in the Cryptocurrency Market under Realistic Assumptions*, SSRN 4675565.
- Dobrynskaya (2024), *Cryptocurrency Momentum and Reversal*, J. Alternative Investments / SSRN 3913263.
- Zarattini, Pagani & Barbon (2025), *Catching Crypto Trends*, Swiss Finance Institute RP 25-80.
- Harvey, Hoyle, Rattray, Sargaison, Taylor & van Hemert (2018), *The Impact of Volatility Targeting*, Financial Analysts Journal.
- Fičura (2023), *Impact of size and volume on crypto momentum and reversal*, VSE/FFA WP.
- Zaremba, Bianchi & Mikutowski (2021), *Up or down? Short-term reversal, momentum, and liquidity in crypto*, Int. Review of Financial Analysis.
- BIS Working Paper No. 1087 (2025), *Crypto Carry*; Fan, Jiao, Lu & Tong, *The Risk and Return of Cryptocurrency Carry Trade*, SSRN 4666425.
- arXiv 2009.12155 (intraday BTC trend); arXiv 2602.10785 (intraday EMA walk-forward).

**Practitioner / exchange (medium–high):**
- Man AHL, *In Crypto We Trend* (2024); Man Group, *Crypto: Too Hot to Handle?*
- QuantPedia, *An Introduction to Volatility Targeting* & *Trend-following and Mean-reversion in Bitcoin*.
- Starkiller Capital, *Cross-sectional Momentum in Cryptocurrency Markets*.
- Stevens Institute FE800, *Improving Grid Trading Strategies in Bitcoin*; Bitsgap / Phemex (grid breakout failure mode).
- StratProof, *I paper-traded 22 strategies on real fees for 10 days*; quant-signals / quantifiedstrategies (EMA & golden-cross backtests).
- PancakeSwap docs (0.25%/swap fee, 0.5–5% auto-slippage) + BSC gas data → ~1–4% real round-trip.

**Repo-internal:**
- [findings.md](findings.md) §15–16 — the inherited ICT/crossover setup has no robust OOS edge after friction; only RR≥2 configs survived; friction dominates tight stops.

---

*Generated from a 14-family adversarial research sweep. The headline finding — that 30–40% in 7 days is not achievable on long-only spot without breaching the DD gate — held unanimously across all 14 verified families. Build for survival; rank by not blowing up.*

---

## 11. Implementation & validation status (auto-generated)

> Wires this playbook's Top-10 families (§3) to the **actually-registered, tested, and validated**
> strategy arms — so the research doc can never silently drift from the code. Regenerated by
> **`make playbook`** (read-only: reads the persisted survival/forward verdicts + stability grades +
> the PnL/win-rate scoreboard; writes only between the markers below). Parity is enforced in CI by
> **`tests/test_playbook_parity.py`**. Families **#1/#2/#7** (trend-pullback / TSMOM / MA-cross)
> collapse onto the momentum + MA-filter arms — their ICT entry was dropped (§6); the core allocator
> is the locked **`momentum_adaptive`**. **Survival is the GATE; PnL/win-rate is the SCOREBOARD**
> (see the VALIDATION UPDATE banner at the top). No edge is claimed — see
> [bnb_strategy_decision.md](bnb_strategy_decision.md) §1.

<!-- PLAYBOOK:START -->

_Auto-generated by `make playbook` — do not hand-edit between the markers. All **10** playbook families map to **10** registered arms (parity in `tests/test_playbook_parity.py`). Survival = GATE; PnL/win-rate = SCOREBOARD (no edge claim). Forward window **5d**. Last run **2026-06-13T21:09:20+00:00**._

| Arm | Family | Survival | Stability | Forward | Backtest total-ret | Win-rate (window) | Forward win-rate (day) |
|---|---|:--:|:--:|:--:|--:|--:|--:|
| `momentum_adaptive` | #1 | ✅ 18.4% DD | FRAGILE | ⏳ prod | -43.3% | 28% | 100% (1/1d) |
| `momentum_mafilter` | #1, #2, #7 | ✅ 24.2% DD | FRAGILE | ⏳ prod | -65.8% | 20% | — |
| `momentum` | #2 | ✅ 18.4% DD | FRAGILE | ⏳ prod | -43.3% | 28% | — |
| `momentum_fast` | #2 | ✅ 18.5% DD | ROBUST | ⏳ prod | -62.1% | 28% | — |
| `dual_momentum` | #3 | ✅ 12.9% DD | ROBUST | ⏳ isolated | -48.0% | 16% | ⏳ accruing |
| `breakout` | #4, #9 | ❌ 31.2% DD | UNSTABLE | ⏳ isolated | -69.1% | 25% | 0% (0/1d) |
| `grid` | #5 | ✅ 21.3% DD | FRAGILE | ⏳ prod | -50.9% | 31% | — |
| `mean_reversion` | #6 | ✅ 14.7% DD | ROBUST | ⏳ prod | -56.6% | 30% | — |
| `rotation` | #8 | ✅ 20.3% DD | FRAGILE | ⏳ prod | -46.9% | 33% | — |
| `momentum_voltarget` | #10 | ✅ 15.0% DD | ROBUST | ⏳ prod | -37.6% | 28% | — |

**Win-rate (window)** = backtest share of rolling 7-day windows positive (`pct_up`). **Win-rate (day)** = live up-days / decided-days from the arm's forward track (matches the dashboard `pnl.ts`); ⏳ accruing until the wall-clock track has resolved days. Backtest total-return is a scoreboard over a long trending sample — **regime luck, not edge** ([bnb_strategy_decision.md](bnb_strategy_decision.md) §1). Refresh inputs: `make campaign` (survival + scoreboard), `make stability` (grades), `make forward_track_all` (forward).

<!-- PLAYBOOK:END -->

---
---

# Part B — Perps & Futures (shorting + leverage) · ⏸ FUTURE WORK (off-TWAK web3)

> **⏸ DEMOTED — NON-SCORING FOR THIS CONTEST (organizer ruling, BNB Chain / Gwen, 2026-06).**
> The contest **only counts trades made through the TWAK swap interface** toward P&L — scoring
> inspects on-chain transactions and counts only swap-interface ones (that's how it tells a
> trade from a deposit deterministically). **Perps can't be tracked that way, so they contribute
> ZERO to P&L and are excluded to keep scoring fair.** That makes everything in Part B
> **research / future work only**, never the live contest strategy — independent of the earlier
> secondary point that TWAK can't sign perps for agents anyway. The shipped strategy is the
> **spot momentum allocator, signed via TWAK** ([bnb_strategy_decision.md](bnb_strategy_decision.md)).
>
> **Scope expansion (historical research framing):** "Perps and Futures should count too as long
> as it's profitable." This part re-runs the entire analysis with **shorting** and **leverage**
> on the table — a second 14-family adversarial sweep (29 agents) that quantifies the
> **return-vs-30%-DD-gate tradeoff** for every family. *(NOTE: the organizer ruling above
> supersedes this section's "venue CONFIRMED → run B4 at 2×" conclusion — perps are non-scoring,
> so B4 is NOT a contest option; it remains here as an audit trail of the research only.)*
>
> **The one-sentence finding:** perps **do not** hand you a free 30–40% — leverage is *Sharpe-neutral*, so it rescales return and drawdown 1:1 and manufactures zero edge; applied to a ~0-edge signal it is pure variance amplification into the DQ line. **Every one of the 14 perp/futures families also returned `plausible_30_40pct_in_7day = FALSE`.**

---

## B0. What perps actually change

Two unlocks, two consequences:

1. **Shorting** revives the 4 families Part A had to exclude — funding-carry, basis, pairs/stat-arb, market-neutral momentum — plus net-new short-side directional plays. **But** every *market-neutral* family needs infrastructure the codebase lacks (see [B5](#b5-neutral-safe-satellite-not-the-engine)).
2. **Leverage** makes 30–40% in 7 days *arithmetically* reachable — and welds that upside to a symmetric, gate-lethal downside.

### The leverage → DD-gate math (memorize this table)

At **N×** leverage on a directional book, a **P% adverse move = N·P% drawdown**. The 30% disqualifier is therefore tripped by:

| Leverage | Adverse move that hits the 30% DQ | Reality check |
|:---:|:---:|---|
| **1×** | 30% | Spot-equivalent; only a major crash. |
| **2×** | **15%** | The "place, don't DQ" config. |
| **3×** | 10% | BTC/ETH do this intraday in a bad week. |
| **5×** (broker default) | 6% | A single ordinary session. |
| **8×** | 3.75% | One candle. |
| **10×** | 3% | One candle. |

**Leverage is Sharpe-neutral** (Van Hemert et al.): it manufactures *zero* risk-adjusted return. So the leverage you'd need to manufacture a 35% week (≈5–10×) is the exact knob whose downside disqualifies you on the first reversal. Feb-2026 printed a **−52% BTC day**; Oct/Nov-2025 saw **~$19–20B liquidation cascades**. At 5–10× those are not "drawdowns," they are liquidations.

---

## B1. The return-vs-DD-risk frontier

Pick your spot consciously. Five verified points, safest → riskiest:

| Profile | Strategy | Expected 7-day return | DD-gate risk |
|---|---|---|---|
| **neutral-safe** | Funding-carry / delta-neutral (1–3×) | **+0.2% … +2%** on capital (often ~0 after friction) | **SAFE** — net delta ~0, no N·P mechanism. Risks are non-price: short-leg liquidation, basis divergence, funding flip. |
| **relative-value** | Pairs / cointegration long-short (1–3×) | **+1% … +4%** (a single breakdown: −3% … −8%) | **SAFE @1–3×** — one-sided spread blowout, not beta. Hard z≥3 stop + basket diversification mandatory. |
| **balanced** ✅ | **Levered trend-following hybrid long+short @2×, 0.5–1% risk** | **−4% … +12%**, centered +2–5% trending | **MODERATE but survivable** — each stopped trade risks ~1–2% equity; needs a ~15% adverse move or a long stop-out streak to threaten the gate. "Place, don't DQ." |
| **balanced-levered** | Levered trend-following @3× | **−25% … +25%** | **DANGEROUS** — at 3× a ~10% adverse move = ~30% DQ; funding drag unmodeled. |
| **aggressive** | Levered directional @5–10× | **−90% … +60%** (the only configs that can print 30–40%) | **GATE-LETHAL** — coin-flip dominated by 1–2 trades; grid/short-momentum are *worse* than naive N·P (inventory growth / squeeze gaps). |

**Your locked choice is the `balanced` row.** That is the headline pick in [B4](#b4-the-balanced-levered-deployable-pick).

---

## B2. Ranked perp/futures families (assuming perps are allowed)

| # | Family | Score | Δ-neutral? | Short? | Build | DD-gate | Honest 7-day return | Verdict |
|---|--------|:---:|:---:|:---:|:---:|:---:|---|---|
| **1** | **Pairs / Statistical Arbitrage** (cointegration L/S) | 5 | ✅ | ✅ | med | **safe** | +1% … +4% | Structurally safest at 1–3×, strong broker reuse — but two-leg execution + venue both unverified. |
| **2** | **Levered Trend-Following hybrid (long+short)** ✅ | 4 | ❌ | ✅ | **low** | moderate | −4% … +12% | **The balanced-levered pick.** Real (peer-reviewed) trend edge; ICT POI/MSS/FVG dropped; posts a double-digit week at controlled leverage. |
| **3** | **Short-Side Momentum / Downtrend Capture** | 4 | ❌ | ✅ | low | dangerous | −8% … +12% | Lowest-build (`SELL`/`fade` already emits) + small funding tailwind, but short-squeeze gap risk. |
| **4** | **Levered Time-Series Trend-Following** | 4 | ❌ | ❌ | low | dangerous | −2% … +12% (2×) | Mechanically lowest-build directional, but a 7-day window is trend's *weakest*-edge horizon. |
| **5** | **Liquidation-Cascade Mean-Reversion** | 4 | ❌ | ❌ | med | dangerous | +2% … +12% (2–3×) | Genuine crypto-native microstructure edge (findings.md flagged it) — but a falling-knife fat left tail. |
| 6 | Volatility Risk Premium / Short-Vol & Funding | 3 | ✅ | ✅ | med | moderate | ~0 … +1% | Real high-Sharpe carry, tiny absolute return; its directional form is a short-vol bomb. |
| 7 | Funding-Rate Carry / Delta-Neutral | 3 | ✅ | ✅ | high | **safe** | +0.2% … +2% | Near-zero-DD edge that fits the 15% target perfectly — but ~0.2–2%/7d and **regime-dead now**. |
| 8 | Cross-Exchange Funding Arbitrage | 3 | ✅ | ✅ | high | moderate | +0.1% … +0.5% | Leverage scales both legs equally → can't amplify the spread; near break-even after fees. |
| 9 | Perp Market-Making / Spread + Funding | 3 | ✅ | ✅ | high | safe | ~break-even | A latency game an hourly **taker** bracket-bot cannot play — it pays the spread it should earn. |
| 10 | Leveraged Perp Grid | 3 | ❌ | ❌ | med | dangerous | range-dependent | Short-vol martingale; high win-rate **defers** losses into inventory that detonates on a breakout. |
| 11 | Market-Neutral Cross-Sectional Momentum | 3 | ✅ | ✅ | high | moderate | ~0 (single draw) | Thin ~0.45-Sharpe single-rebalance lottery — **dead in the current BTC-dominant low-dispersion regime.** |
| 12 | Cash-and-Carry Basis Trade | 2 | ✅ | ✅ | high | safe | +0.2% … +1% | Edge is **regime-dead** (Dec-2025 backwardation); needs dated/quarterly contracts BSC perp DEXs lack. |
| 13 | Calendar / Basis Spread | 2 | ✅ | ✅ | high | safe | ~0 | Per-annum carry with **no quarterly expiry inside a 7-day window** to anchor convergence. |
| 14 | Delta-Neutral Yield (carry+basis+staking) | 2 | ✅ | ✅ | high | **safe** | <+1% | Safest book in the set (<2% historic DD) but well under 1% over 7 days; spot/staking legs unbuilt. |

**Pattern:** the **safe** families (1, 7, 9, 12–14) can't move the leaderboard in 7 days; the families that *can* post double digits (2–5, 10) all carry **moderate-to-dangerous** gate risk. There is no free lunch — that's the whole story.

---

## B3. Per-family deep dives (key facts + cited evidence)

### #1 Pairs / Statistical Arbitrage (cointegration long/short) — Score 5, DD **safe**
- **Mechanism.** Long/short a cointegrated pair (e.g. ETH/BTC, or an L1 basket) on the spread z-score; market-neutral, profit on mean-reversion of the spread.
- **Evidence.** *Statistical Arbitrage Using Cointegration in Crypto* (medium cred) — BTC-ETH pairs Sharpe ~2.45, ~16.3% annualized; *Pairs Trading in the Cryptocurrency Market* (Erasmus) — cointegrated pairs generate risk-adjusted excess returns but edge compresses with crowding.
- **Realistic.** ~8–20% annualized net of fees+funding for a disciplined book; **7-day +1–4%**, with a single cointegration breakdown turning the week −3% to −8%.
- **Codebase.** Reuse the SELL-side brackets + caps. **Build:** a two-leg/net-delta coordinator — `orders.py` models one pair/one side/one bracket, so a partial/failed fill on ONE leg leaves a **naked** position that silently destroys the neutrality the whole safety case rests on. **Riskiest build item.**
- **Verdict.** Safest *return-bearing* perp play — but venue-unverified and the two-leg coordinator is net-new.

### #2 Levered Trend-Following hybrid (long+short) — Score 4, DD **moderate** — ⏸ FUTURE WORK (was "THE PICK"; demoted — TWAK can't sign perps)
- **Mechanism.** Part A's trend-following core (`bias_sma`∧`bias_slope` agree) + pullback-to-MA entry, run on perps with the **short side** enabled (`Order.side=SELL`) when the 4h trend is down. RR≥2 floor. **ICT POI/MSS/FVG dropped** (no OOS edge).
- **Evidence.** [findings.md §12–16](findings.md) — the base ICT setup has **NO OOS edge** (only RR≥5 survived); He & Manela, *Fundamentals of Perpetual Futures* (arXiv 2212.06888) — realistic BTC perp Sharpe ~1.8 under retail-grade assumptions *when an edge exists*.
- **Realistic.** Unlevered, ~0 to slightly positive after 0.14% round-trip; at **2× / 0.5–1% risk / RR≥2 → −2% to +12%/7d**, centered near break-even-minus-funding.
- **Codebase.** **Lowest build** — mostly config: reuse 3-leg brackets + ISOLATED + BUY/SELL (testnet-proven, +5.02R XRP live), `follow`/`fade`, `bias_*`, `regime`, caps. **Build:** set `max_drawdown_frac≈0.15`; **add funding-cost accounting** (held 7-day perp pays ~0.2–1.5% funding, currently unmodeled); leverage/notional cap. *The real gap is EDGE, not execution.*
- **Verdict.** The balanced-levered choice. Full spec in [B4](#b4-the-balanced-levered-deployable-pick).

### #3 Short-Side Momentum / Downtrend Capture — Score 4, DD **dangerous**
- **Mechanism.** Short weak/breaking-down coins in a confirmed downtrend; you also *earn* funding shorting positive-funding coins.
- **Evidence.** Han, Kang & Ryu — TS momentum premium is **concentrated in winners**; the short-loser leg is risky. Drogen, Hoffstein & Otte — a 30-day winner/loser anomaly persists ~7 days, but the short side squeezes.
- **Realistic.** −10% to +25% standalone after fees; at 2× → −8% to +12%/7d.
- **Risk.** Reduce-only stop-market fills at the **squeeze candle**, not the trigger — backtests assuming stop-price fills understate left-tail DD. The single best squeeze-avoidance signal (funding feed) is **unbuilt**.
- **Verdict.** Cheap to add as the bearish half of #2, but the short tail is genuinely dangerous — keep leverage ≤2× and stops hard.

### #4 Levered Time-Series Trend-Following — Score 4, DD **dangerous**
- **Evidence.** Grayscale, *The Trend is Your Friend* — MA trend captures BTC upside while cutting tail exposure; arXiv 2602.11708 — best-case adaptive crypto trend Sharpe 2.41 / Calmar 3.18 / maxDD −12% (full cycle, **not** 7 days).
- **Realistic.** 15–40%/yr in trending regimes; a 7-day window is trend's *weakest* expression horizon. Funding-cost-blind in the current code.
- **Verdict.** Lowest-build directional, but the horizon mismatch + dangerous DD at the leverage needed to matter keep it behind #2.

### #5 Liquidation-Cascade Mean-Reversion — Score 4, DD **dangerous**
- **Mechanism.** Fade violent liquidation-driven wicks (mean-revert the over-shoot) with a deliberately **wide** stop and a time-based exit.
- **Evidence.** Curupira.dev (1m dual-filter velocity-z + 3× volume fade) — survives with tight risk control; Tigro Blanc (50k+ hours of data) — concluded the edge is marginal after costs.
- **Risk.** Wide stop = a continuation move is the dominant P&L driver = an outsized single loss is the core risk. The leverage needed to hit the target (8–15×) makes a normal 2–4% wick-extension a 16–60% DD.
- **Verdict.** Genuine microstructure edge (findings.md flagged it as a "next idea"), but the falling-knife tail makes it a research project, not a 7-day deploy.

### #6–14 (condensed)
- **VRP / Short-Vol & Funding (3):** delta-neutral carry ~8–16%/yr net at Sharpe 4–6 *but tiny absolute return*; the only contest-relevant form is 6–10× directional funding — a short-vol bomb. (Skyler Chan, SSRN leveraged funding-carry.)
- **Funding-Carry / Delta-Neutral (3, safe):** real structural edge, <2% DD by design — but **BIS WP 1087** shows carry *predicts crashes* (it's compensation for tail risk), **Fan et al. SSRN 4666425** show the cross-sectional version loads on the equity vol-risk-premium. **Regime-dead now** (compressed/backwardated basis).
- **Cross-Exchange Funding Arb (3):** 5–20% APY in good regimes (ScienceDirect; Pendle/Boros: Binance vs Hyperliquid 6.4–11.4% APR) — but leverage can't amplify a net-zero book; ~0.1–0.5%/7d.
- **Perp Market-Making (3):** retail taker MM is break-even-to-negative after adverse selection (Multicoin, *Adverse Selection Rules Everything Around Me*; Hummingbot 7-day live). An hourly-bar bracket bot **cannot** play this latency game.
- **Levered Perp Grid (3, dangerous):** Quantpedia calls grid "a textbook martingale" — high win-rate **defers** loss into inventory that detonates on a breakout.
- **MN Cross-Sectional Momentum (3):** Liu/Tsyvinski/Wu (JoF) price momentum as a factor, but Starkiller's L/S replication is only ~11.7% full-sample EW — and it's **regime-dead** at BTC.D ~56–60% / altseason ~35.
- **Basis Cash-Carry (2) & Calendar Spread (2):** CMU *Crypto Carry* shows Sharpe 6.45 (2020–25) **falling**; both need dated/quarterly contracts that BSC perp DEXs (ApolloX/Level) **don't list** — fatal venue mismatch, and no quarterly expiry sits inside a 7-day window.
- **Delta-Neutral Yield combo (2, safe):** the Ethena-style book (<2% DD) — but <1% over 7 days and the spot/staking legs are unbuilt.

---

## B4. The balanced-levered deployable pick

**→ Diversified Trend-Following hybrid (long + short), 2× on the BSC perp DEX.**

> The §6 trend-following core run on the **BSC perp DEX** with the **short side enabled** and **2× leverage** — long+short across the basket = **12–16 independent legs**. Same pandas-pure trend signal, same DD machinery; the ICT POI/MSS/FVG entries stay **deleted**.

**Entry.** Per coin on **4h**: long when `bias_sma` ∧ `bias_slope` agree up, short when both agree down (skip on disagreement / `LOW_VOL`). Timing = pullback to the bias-MA, enter on the in-trend resumption bar. Long via `Order.side=BUY`, short via `SELL`. **No POI/MSS/FVG/delta.**

**Exit.** Reduce-only `STOP_MARKET` SL (ATR-anchored ≈1.5–2.0×) + reduce-only `LIMIT` TP at ≥2R + **trend-flip exit** + trail-to-BE after +1R. Poll-and-exit (a DEX gaps between polls); `STOP_MARKET` fills at *market* on a gap, so realized loss scales 1:N with leverage — keep N low.

**Sizing & leverage.** ATR vol-targeting (`sizing.position_size`), **0.5–1% risk per leg**, **half-Kelly as an upper clamp only**. **Hard 2× cap** (3× absolute ceiling, never the broker 5× default — leverage is Sharpe-neutral). DD math (from [B0](#b0-what-perps-actually-change)): 2×→15%, 3×→10%, 5×→6% adverse move = 30% DQ.

**Expected outcome.** At 2× / 0.5–1% risk / RR≥2: **−4% to +12% over 7 days**, centered **+2–5%** trending / ~flat chop; fat right tail with shorts enabled in a clean directional week. DD-gate **safe by design** (vol-target + 2× cap + ATR stops + `MaxDrawdown` 0.15 + trend-flip exit). Will not DQ; the live risk is a chop week printing flat, not a blow-up.

**Codebase changes.** Reuse: `bias_sma`/`bias_slope`/`structure`, `atr`, `regime`, `sizing`, all caps, `Account`, `Order`/`PaperBroker` (`Side` already `BUY`/`SELL`; `realised_pnl_R` handles both directions). **Build-new:** `trend_basket.py`; the **strategy-agnostic WFO replay loop** (the gating item — `wfo.py` is POI-bound); the BSC perp DEX broker (long+short, reduce-only) + PancakeSwap spot broker, both signing via **TWAK**; the **funding feed + funding-accrual P&L** (`grep funding src/` = 0 hits; held perps pay ~0.2–1.5%/7d) + a long-side funding-skip; set `MaxDrawdown` 0.20→0.15 + the 2× clamp. **Delete from the live path:** the POI/MSS/FVG/delta AND-gate. **The gate is EDGE — validate via Gate A before levering; if it fails, ship the §6 spot long-only 1× fallback, never the leveraged variant unproven.**

---

## B5. Neutral-safe satellite (NOT the engine)

If you want a tiny DD-stabilizing sleeve alongside the directional driver, the structurally correct choice is **funding-carry / pairs**: near-zero directional drawdown by construction, the safest way to clear ≥7 trades and post positive. **But deploy it only as a small satellite, never as the engine** — its 7-day ceiling at safe leverage is **~0.2–2%**, it's **regime-dead right now**, and its *true* home is your broader always-on Binance/Delta yield product.

**Honest build cost (high, despite the short broker existing):** the SELL-side bracket is free, but you must build (1) a **funding-rate feed + cache** (zero funding refs in `src/`); (2) a **funding-percentile signal** (`ICTProMaxStrategy.evaluate()` is structure-based, not funding-based); (3) a **two-leg net-delta position + hedge-ratio rebalancer** (single-leg `Order`/3-leg bracket is the wrong shape); (4) **funding-accrual P&L** (`Account.book_close` is strictly R-multiple off SL distance — it *cannot* represent a no-SL carry position); (5) a negative-funding kill-switch.

---

## B6. The honest 30–40%-with-leverage verdict

**Yes, 30–40% in 7 days is now *arithmetically* possible — but it's closer to a coin-flip than a strategy.** Every market-neutral family is out as a *return engine* (their edge is collected on net notional, so leverage scales both legs equally — you'd need ~30–200× to reach the target, which isn't delta-neutral anymore, it's a liquidation bomb). So the target **forces a directional perp at ~5–10×**. At that leverage, a routine 3–6% session disqualifies you. Two compounding problems make it worse than the bare arithmetic: (1) [findings.md](findings.md) proves the directional signal has **~0 OOS edge** — leverage multiplies a ~0 edge by N while multiplying tail risk by N; (2) leverage is **Sharpe-neutral** — zero risk-adjusted return for the gate risk it imports. **Do not chase 30–40%.** The defensible play under a 15% self-cap is the **2× disciplined directional book targeting single-digit-to-~12% with a hard stop** — "place mid-pack, don't DQ" — which is exactly what [bnb_strategy_decision.md](bnb_strategy_decision.md) already concludes. If a competitor hits 35%, they did it by accepting a high probability of disqualification instead.

---

## B7. Spot vs. Perp — the decision (FINAL: spot, perps non-scoring)

**Organizer ruling (2026-06): only TWAK-swap-interface trades count toward contest P&L, so perps score ZERO and the contest path is the spot momentum allocator ([Part A](bnb_strategy_decision.md)) — NOT the perp pick.** The analysis below is retained as the research record of what perps *would* change if they were scoreable; it is no longer a contest recommendation:

**What perps WOULD change (historical research — non-scoring per the organizer ruling):**
1. **The opportunity set doubles** — the short side is unlocked, so the agent can profit in down-legs and chop, not only uptrends. On the existing engine the short is a near config-flip (`strategy_mode='fade'` / `Order.side=SELL`, testnet-proven with a real +5.02R XRP TP).
2. **Disciplined leverage is available** — at 2× (never >3×) a genuine edge expresses faster while staying inside the 15% DD budget.

**What it does NOT change (the caveats still bind):**
3. **The critical lift is the BSC perp-DEX broker + funding feed — both entirely unbuilt.** A DEX-perp broker (ApolloX / Level / equivalent) implementing `broker.py`; a funding-rate feed (`grep funding src/` = 0 hits); funding-accrual P&L (`Account.book_close` is R-multiple/SL-based and can't represent funding yet). This is now the **top of the critical path** — budget it before anything else.
4. **The leverage/DQ trap is real** — every reason from [B0](#b0-what-perps-actually-change) and the [B6](#b6-the-honest-3040-with-leverage-verdict) verdict stands. Cap leverage at **2×**, DD at **15%**. Do not chase 30–40%.
5. **The real gap is EDGE, not execution** — [findings.md](findings.md) shows the *ICT* signal is ~0 OOS, which is why it was dropped. The trend-following signal must still clear Gate A (TRAIN>0 ∧ TEST>0 on real 4h data) via the new agnostic WFO replay before levering anything; if it fails, ship the spot 1× fallback.

**Net:** the contest runs **B4 (trend-following hybrid) on the BSC perp DEX at 2×**, with the spot trend-following (1×) armed as a **gate-safe fallback** (ship it instead if the DEX broker isn't production-ready by the freeze date, if the live regime is hostile to leverage, or if the leveraged variant fails Gate A). The broader always-on autotrade product still inherits the full perp playbook — carry/trend compounding with no 7-day gate.

---

## B8. Venue resolved

**⚠️ SUPERSEDED (organizer ruling, 2026-06):** an earlier organizer note said perps were
"permitted in-venue." The **scoring rule overrides that for the contest**: only trades through
the **TWAK swap interface** are counted toward P&L (deterministic trade-vs-deposit detection),
so perps — which can't be tracked that way — **score zero**. Part B is therefore **NOT live for
the contest**; it is retained as research/future-work only. The live strategy is spot via TWAK.

**Resulting critical path (in order — this is the build queue):**
1. **BSC perp-DEX broker** (ApolloX / Level / equivalent) implementing the `broker.py` protocol with reduce-only stop/TP emulation + **poll-and-exit** (a perp DEX can gap between 1h polls). The single biggest unbuilt item — it is the new top of the critical path.
2. **Funding-rate feed + funding-accrual P&L** — `grep funding src/` = 0 hits, and `Account.book_close` is R-multiple/SL-based, so a held perp's funding currently hits neither the equity curve nor the backtest. Build both before any levered backtest is trustworthy.
3. **Set `max_drawdown_frac ≈ 0.15` + a hard 2× leverage cap** (3× absolute ceiling) before any live capital.
4. **Re-cost + WFO the trend-following signal at BSC-perp-DEX friction** (the new agnostic replay loop + Gate A). The real gap is EDGE — do not lever a signal that hasn't cleared the OOS gate.
5. **Deploy [B4](#b4-the-balanced-levered-deployable-pick) at 2×**, with the spot trend-following (1×) armed as the gate-safe fallback.

> Spot (Part A) is no longer the *only* contest path, but it stays the **gate-safe fallback** — if the DEX broker isn't production-ready by the freeze date, ship spot rather than push an untested leveraged broker into a single-shot, 7-day, DD-gated window.

---

## B9. Added references (Part B)

- BIS Working Paper No. 1087 (2025), *Crypto Carry* — carry >10%/yr, **predicts crashes**.
- Fan, Jiao, Lu & Tong, *The Risk and Return of Cryptocurrency Carry Trade*, SSRN 4666425 — cross-sectional carry 43.4%/yr at Sharpe 0.74, loads on equity VRP.
- He & Manela et al., *Fundamentals of Perpetual Futures*, arXiv 2212.06888.
- Han, Kang & Ryu (2023), *TS & CS Momentum in Crypto under Realistic Assumptions*, SSRN 4675565.
- Liu, Tsyvinski & Wu (2022), *Common Risk Factors in Cryptocurrency*, Journal of Finance.
- Grayscale Research, *The Trend is Your Friend: Managing Bitcoin's Volatility*; arXiv 2602.11708 (adaptive crypto trend).
- Quantpedia, *A Primer on Grid Trading Strategy* (martingale framing); Multicoin Capital, *Adverse Selection Rules Everything Around Me*.
- CMU, *The Crypto Carry Trade*; CF Benchmarks, *Revisiting the Bitcoin Basis*; Curupira.dev, *Fading Liquidation Cascades*.
- Current-regime data (verified): BTC dominance ~56–60%, altseason index ~35, BTC 3M basis ~2.5% (intermittently backwardated) — the regime in which market-neutral dispersion edges are weakest.
- Repo-internal: [findings.md](findings.md) §12–16 (no OOS edge; only RR≥2 survived friction); [bnb_strategy_decision.md](bnb_strategy_decision.md) (spot, ≤15% DD, "place + win TWAK special").

---

*Part B generated from a second 14-family adversarial sweep with a leverage + short + DD-gate-math lens. The unanimous finding holds: perps unlock shorting (reviving 4 families) and make 30–40% arithmetically possible, but leverage is Sharpe-neutral — it buys zero edge for the gate risk it imports. Balanced-levered (2× trend-following hybrid, both directions, DD ≤15%) is the disciplined perp play. **FINAL (organizer ruling, 2026-06): perps are NON-SCORING — only TWAK-swap-interface trades count toward contest P&L — so the contest runs the spot momentum allocator, NOT B4. Part B stands as research/future-work only.***
