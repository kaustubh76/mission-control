{
  "name": "mission-control-site",
  "private": true,
  "version": "0.1.0",
  "description": "Vercel build wrapper — builds the web/ Vite SPA for static hosting. (The app itself is a Python project; this file only exists so Vercel builds the dashboard.)",
  "scripts": {
    "build": "cd web && npm ci && npm run build"
  }
}
